# Automation and Bash Basics

#!/bin/bash  
Defines the script interpreter and ensures the script runs using Bash.

echo "message"  
Prints output to the terminal, commonly used for logs and status messages.

for i in {1..5}; do ... done  
Executes a loop, often used for repetitive automation tasks.

if [ -f file ]; then ... fi  
Checks for file existence before executing dependent logic.
